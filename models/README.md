# Model Weights

This directory is intentionally kept out of git for large checkpoints.

If you need the I3D RGB ImageNet weights used by `detection_and_source_tracing/i3d.py`,
download them on demand:

```bash
uv run python scripts/download_i3d_weights.py
```
